#include <iostream>
#include <bits/stdc++.h> 
using namespace std;

int main() {
    stack <int> s;
    int numOfTestCases;
    cin >> numOfTestCases;
    int arrSize;
    cin >> arrSize;
    int inpArray[arrSize];
    for(int i = 0; i < arrSize; i++) {
        scanf("%d",&inpArray[i]);
    }
	return 0;
}